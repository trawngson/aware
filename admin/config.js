// The Supabase project the admin page talks to. Fill in both values (see
// supabase/README.md). The anon (publishable) key is public by design; never
// put the service-role (secret) key here: this file is published with the page.
window.AWARE_ADMIN_CONFIG = {
  supabaseUrl: "",
  supabaseAnonKey: "",
};
